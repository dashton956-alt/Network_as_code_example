intent:
  id: corp-mgmt-ntp-all-sites-001
  type: mgmt_ntp
  version: 1
  tenant: acme-corp
  change_ticket: CHG0012356
  description: "NTP configuration — all network devices — primary and secondary NTP servers"

  scope:
    all_tenant_devices: true

  management:
    ntp_servers:
      - "10.0.0.1"
      - "10.0.0.2"
      - "pool.ntp.org"

  policy:
    compliance: NONE
    encryption: none

<<<<<<< HEAD
  deployment:
    controller: nornir
    strategy: rolling
    verification:
      level: basic
      trigger: on_deploy
      fail_action: alert
=======
  verification:
    level: nuts
    trigger: on_deploy
    fail_action: alert
    tests:
      - test_class: TestNapalmConfig
        label: "Verify NTP servers in running config"
        test_data:
          - host: lab-arista-sw01
            expected:
              - config_snippet: "ntp server 10.0.0.1"
              - config_snippet: "ntp server 10.0.0.2"
          - host: lab-arista-sw02
            expected:
              - config_snippet: "ntp server 10.0.0.1"
              - config_snippet: "ntp server 10.0.0.2"
>>>>>>> 256ce52 (feat: add verification section to multiple intents for enhanced testing capabilities)
